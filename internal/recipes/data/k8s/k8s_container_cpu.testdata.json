{
  "_recipe": "k8s_container_cpu",
  "_comment": "Split child of k8s_container_resources (T6A.2). Exact name match on container_cpu_usage_seconds_total counter; rate over 5m grouped by namespace+pod+container, excluding machine-level rows with empty container/pod labels.",

  "positive_metrics": [
    {
      "name": "container_cpu_usage_seconds_total",
      "type": "counter",
      "labels": ["container", "namespace", "pod"],
      "traits": []
    }
  ],

  "negative_metrics": [
    {
      "_comment": "Look-alike: working-set memory gauge (handled by sibling split k8s_container_memory).",
      "name": "container_memory_working_set_bytes",
      "type": "gauge",
      "labels": ["container", "namespace", "pod"],
      "traits": []
    },
    {
      "_comment": "Look-alike: unrelated cAdvisor counter — name not container_cpu_usage_seconds_total.",
      "name": "container_network_receive_bytes_total",
      "type": "counter",
      "labels": ["container", "namespace", "pod"],
      "traits": []
    },
    {
      "_comment": "Look-alike: gauge type is rejected by type predicate.",
      "name": "container_cpu_usage_seconds_total",
      "type": "gauge",
      "labels": ["container", "namespace", "pod"],
      "traits": []
    }
  ],

  "expected_panels": [
    {
      "title_contains": "Container CPU: container_cpu_usage_seconds_total",
      "query_contains": "rate(container_cpu_usage_seconds_total{container!=\"\", pod!=\"\"}[5m])",
      "kind": "timeseries"
    }
  ]
}
