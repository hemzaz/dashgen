{
  "_recipe": "infra_disk",
  "_comment": "Match keys on node_filesystem_avail_bytes; pair_with: suffix_swap _avail_bytes->_size_bytes; literal ratio query.",

  "positive_metrics": [
    {
      "name": "node_filesystem_avail_bytes",
      "type": "gauge",
      "labels": ["device", "fstype", "instance", "mountpoint"]
    }
  ],

  "pair_metrics": [
    {
      "_comment": "Pair partner — required for pair_with: suffix_swap to resolve.",
      "name": "node_filesystem_size_bytes",
      "type": "gauge",
      "labels": ["device", "fstype", "instance", "mountpoint"]
    }
  ],

  "negative_metrics": [
    {
      "_comment": "Look-alike: size-side gauge alone never matches (Match keys on _avail_bytes).",
      "name": "node_filesystem_size_bytes",
      "type": "gauge",
      "labels": ["device", "fstype", "instance", "mountpoint"]
    },
    {
      "_comment": "Look-alike: inode counter — different family.",
      "name": "node_filesystem_files",
      "type": "gauge",
      "labels": ["device", "fstype", "instance", "mountpoint"]
    },
    {
      "_comment": "Look-alike: counter type with same base name is rejected.",
      "name": "node_filesystem_avail_bytes",
      "type": "counter",
      "labels": ["device", "fstype", "instance", "mountpoint"]
    }
  ],

  "expected_panels": [
    {
      "title_contains": "Disk used ratio",
      "query_contains": "1 - (node_filesystem_avail_bytes / node_filesystem_size_bytes)",
      "kind": "timeseries"
    }
  ]
}
