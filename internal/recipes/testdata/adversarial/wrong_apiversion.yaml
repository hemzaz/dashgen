# adversary: T16 — wrong apiVersion (loader must reject)
apiVersion: dashgen.io/v0
kind: Recipe
metadata:
  name: wrong_apiversion
  section: traffic
  profile: service
  confidence: 0.5
  tier: v0.3
match:
  type: counter
panels:
  - title_template: "x"
    unit: ops/sec
    query_template: "rate(foo[5m])"
    legend_template: "x"
